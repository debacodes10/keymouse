[build]
# Windows App Control blocks executing Cargo build scripts from the Downloads tree.
# Redirect artifacts to a writable trusted directory so plain cargo commands work.
target-dir = "C:/Users/xzzy/.codex/memories/keymouse-target"
